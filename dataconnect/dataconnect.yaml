specVersion: "v1beta"
serviceId: "kithkin"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "kithkin-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
